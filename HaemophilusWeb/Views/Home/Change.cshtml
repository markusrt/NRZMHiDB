@using HaemophilusWeb.Models
@using HaemophilusWeb.Utils
@using HaemophilusWeb.Views.Utils
@model HaemophilusWeb.Models.Change

@{
    ViewBag.Subtitle = "Änderung vom " + Model.Date.ToReportFormat();
}
<p>
    <h2>
        @if (Model.Type == ChangeType.Bug)
        {
            <span class="badge text-bg-danger">Fehlerbehebung</span>
        }
        else if (Model.Type == ChangeType.Feature)
        {
            <span class="badge text-bg-info">Neue Funktion</span>
        }
        else if (Model.Type == ChangeType.Design)
        {
            <span class="badge text-bg-success">Design</span>
        }
        <span class="badge text-bg-primary">@EnumEditor.GetEnumDescription(Model.Database)</span>
    </h2>
</p>
<p>
    @Html.Raw(Model.Details.Replace("~/", Url.Content("~/")))
</p>